{
  "id": "FC-004",
  "category": "fc-check",
  "description": "Tool with PURE sentinel and FS side-effect — contradiction, must be rejected",
  "level": 99,
  "spec_version": "0.8",
  "input": [
    {
      "type": "function",
      "function": {
        "name": "bad_tool",
        "description": "Incorrectly marked as both PURE and FS",
        "parameters": {
          "type": "object",
          "properties": {
            "path": {"type": "string"}
          }
        },
        "effects": ["PURE", "FS"]
      }
    }
  ],
  "expected": "CHECK_ERROR",
  "error_code": "CHECK_ERROR",
  "tags": ["pure-contradiction", "fc"]
}
